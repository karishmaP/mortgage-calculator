import { DecimalDirectiveDirective } from './decimal-directive.directive';

describe('DecimalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
